<%= form_with(model: dex, local: true) do |form| %>
  <% if dex.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dex.errors.count, "error") %> prohibited this dex from being saved:</h2>

      <ul>
      <% dex.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :numero %>
    <%= form.text_field :numero %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
